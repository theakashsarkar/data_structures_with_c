#include<bits/stdc++.h>
using namespace std;
int arr[10000];
mt19937 rng

void  bubbleSort(int n)
{
    for(int i = 0; i< n; i++)
    {
        for (int  j = i + i; j < n; i++)
        {
            if(arr[i] > arr[j])
            {
                swap(arr[i], arr[j]);
            }
        }
        
    }
}
int main()
{
    int n = 10;
    for(int i = 0; i < n; i++)
    {
        
    }
}
